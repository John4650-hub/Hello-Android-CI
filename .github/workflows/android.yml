name: Learning linux
on:
  push:
    branches: [ "main" ]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: First job
      run: ls
    - run: chmod +x HelloLinux.sh && ./HelloLinux.sh
