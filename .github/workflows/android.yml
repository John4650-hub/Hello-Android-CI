name: Learning linux

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: First job
      run: ls
    - run: chmod +x HelloLinux.sh && ./HelloLinux.sh
