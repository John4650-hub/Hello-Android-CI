version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.15
    steps:
      - run:
        name: My first circle ci build
        command: |
          echo 'hello circle ci'
          echo 'we are here in Africa'